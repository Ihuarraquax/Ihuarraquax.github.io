{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Edytowalne, wersjonowalne CV (dane w YAML -> HTML + PDF)",
  "scripts": {
    "build": "node src/build.mjs",
    "pdf": "node src/build.mjs && node src/pdf.mjs",
    "serve": "node src/serve.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
